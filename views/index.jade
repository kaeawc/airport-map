doctype 5
html
  head
    title= title
    link(rel='stylesheet', href='/css/reset.css')
    link(rel='stylesheet', href='/css/style.css')
    link(rel='stylesheet', href='/css/scrollbar.css')
    link(rel='stylesheet', href='/css/map.css')
    script(type="text/javascript",src="http://maps.google.com/maps/api/js?sensor=false&libraries=geometry,places,")
    script(type='text/javascript', src='./js/ajax.js')
    script(type='text/javascript', src='./js/map.js')

  body(onresize='map.resize();')
    #map
    #map-search
    		#map-search-bar(class='text-input',contentEditable='true',onfocus='if(this.innerText=="Enter address, business, or landmark")this.innerText="";',onblur='if(this.innerText == "") this.innerText="Enter address, business, or landmark";') Enter address, business, or landmark


  script(type='text/javascript')
    var map = new Map();
    window.onload = function() {
      map.render();
    }